---
plugin: aws_ec2
regions:
  - eu-west-1

# filters:
# All instances with their `Name` tag set to `Target`
#  tag:Name: lw-lab-01
hostnames:
  - tag:Name
  - private-ip-address
strict_permissions: false
